# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.env
*.egg-info/
dist/
build/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Optional: Some prefer to commit this, others don't for libraries. For apps, it's good to commit.
                  # Since this is more of an application/script, let's keep it (remove this line from .gitignore if you want to commit it).
                  # For open source utility scripts, often package-lock is gitignored so users get latest compatible versions.
                  # Let's decide to commit package-lock.json for reproducibility. So, don't add it here.

# Output files
translated_products_test.json
*.log
chromedriver.log # Just in case

# Hugging Face Cache (if models are cached inside project, default is user's home)
.cache/

# OS generated files
.DS_Store
Thumbs.db